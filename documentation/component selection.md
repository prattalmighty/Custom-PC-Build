Requirements → constraints → selection → compatibility

For example:

The primary requirements were photo editing and gaming while staying within a defined budget. PCPartPicker was used to identify compatible components and compare options.
